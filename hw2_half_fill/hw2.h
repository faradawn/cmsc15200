// returns the initial letter of a corresponding 0-9 digit
unsigned char first_letter(unsigned int digit);

// approximates pi with circle's area in a grid with a number of slices
double calc_pi(unsigned long long int nslices);

// computes the square root of s by refining the guesses until max_iter reached
double bakhshali(double s, double guess, double epsilon, unsigned int max_iters);

// draws a half-filled square with given side length and target part
void half_filled_square(unsigned int side_length, int upper_right);



